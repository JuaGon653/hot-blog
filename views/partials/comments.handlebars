<div class="container col-12 p-5">
    <div class="card">
        <div class="card-top">
        </div>
        <p>{{content}}</p>
        <h6>&nbsp;- {{user.username}}, {{format_date date_created}}</h6>
    </div>
</div>