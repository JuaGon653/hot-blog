<div class="container col-12 p-5">
    <div class="card">
        <div class="card-top">
            <h1>{{blog.title}}</h1>
            <h3>{{blog.date_created}}</h3>
        </div>
        <p>{{blog.content}}</p>
    </div>
</div>