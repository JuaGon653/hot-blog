<div class="some-card container col-12 p-5">
    <a href={{view_post_link blogId}}>
        <div class="card">
            <div class="card-top">
                <h1>{{title}}</h1>
                <h3>Posted by {{user.username}} on {{format_date date_created}}</h3>
            </div>
            <p>{{content}}</p>
        </div>
    </a>
</div>